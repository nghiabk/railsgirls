<h1>List users</h1>

<% @ideas.in_groups_of(3) do |group| %>
  <div class="row">
    <% group.compact.each do |idea| %>
      <div class="col-md-2">
        <%= image_tag idea.picture_url(:thumb) if idea.picture? %>
        <h4><%= link_to idea.name, idea %></h4>
        <%= idea.descriptiontion %>
      </div>
    <% end %>
  </div>
<% end %>